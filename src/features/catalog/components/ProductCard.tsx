/**
 * features/catalog/components/ProductCard.tsx
 *
 * ⚠️  APEX Sprint (U1): This file is now a re-export shim.
 * The CANONICAL implementation lives at:
 *   @/components/commerce/ProductCard.tsx
 *
 * All existing import sites continue to work — no changes needed in callers.
 * Fixes: B4 (duplicate ProductCard implementations) from implementation_plan.md
 */

export { default, ProductCard } from "@/components/commerce/ProductCard";
export type { ProductCardProps, UnifiedProductCard } from "@/components/commerce/ProductCard";
