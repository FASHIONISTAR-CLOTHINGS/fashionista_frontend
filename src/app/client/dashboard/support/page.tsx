import { ClientSupportView } from "@/features/client";

export const metadata = {
  title: "Support Tickets — Fashionistar",
  description: "Create and track support tickets for any issues with your Fashionistar orders or account.",
};

export default function SupportPage() {
  return <ClientSupportView />;
}
